# Terraform working files (never publish these)
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
tfplan
*.tfplan
destroyplan
crash.log
crash.*.log
application_default_credentials.json

# Lab 3.3 Path B fixture plan (hand-authored; not a live terraform export)
terraform/primitives/policy-fixture/plan.json

# OS noise
.DS_Store

# Trestle authoring workspace (Lab 6.1). Finished JSON is copied to oscal/.
.trestle-work/
